# Security policy

AERA plugins may run during a privileged recovery session. Report signature,
installer, sandbox, or update-channel issues privately to the organization
owners before public disclosure.

The signing private key must never be committed to GitHub. Rotate the public
key through an AERA recovery update if compromise is suspected.
